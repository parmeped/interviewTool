using System;
using System.Collections.Generic;
using System.Text;

namespace InterviewTool.src
{
    public class Constants
    {
        public const string
            Separator = "------------------------",
            GradeOutsideOfRange = "The grade set ({0}) is outside of the grade range. Try again!";
        
        public const ConsoleKey SkipKey = ConsoleKey.S;
    }
}
